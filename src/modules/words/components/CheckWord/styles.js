import styled from 'styled-components';
import { Button } from 'antd';

export const CheckWordWrapper = styled.div`
  display: flex;
  margin-left: 10px;
  width: 355px;
`;

export const CheckButton = styled(Button)`
  margin-left: 15px;
`;
