import { Icon } from 'antd';
import styled from 'styled-components';

export const ExcelIcon = styled(Icon)`
  position: fixed;
  bottom: 80px;
  right: 25px;
  font-size: 34px;
  cursor: pointer;
  background: #fff;
`;
